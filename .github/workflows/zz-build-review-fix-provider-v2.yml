name: Build and publish provider review fixes v3

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'publisher preparation in progress'
